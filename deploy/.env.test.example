# Test stack host binding overrides
# Copy to .env.test in the runtime deploy directory if you want to customize them.

TEST_BIND_HOST=127.0.0.1
TEST_SERVER_PORT=18080
TEST_IMAGE_TAG=sub2api:monitor-group-filter-test
TEST_DATA_DIR=data-test
